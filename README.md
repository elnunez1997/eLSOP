# eLSOP — Enhanced Link Support Operations Portal

A centralized knowledge and operations portal for ACOSTA support agents, built as a Python-generated static site and hosted on GitHub Pages.

## What it contains

- **Homepage** (`index.html`) — search, department cards, recently updated SOPs, bookmarks, dark mode
- **SOP Library** (`library.html`) — grid/list view, filter by department/category/status/owner, sort, full-text search
- **SOP Reader** (`reader/*.html`) — book-style reading experience with TOC, section collapse, progress bar, download, print
- **Dashboard** (`dashboard.html`) — KPI cards, department/status charts, most viewed, recently updated
- **Customer Matrix** (`customers.html`) — searchable, filterable table of all customers from `Customer_Matrix.xlsx` with signon codes, customer type, claims processing info, backup retrieval, portal status, and formal SOP links. Click any row for a full detail drawer.
- **Formal SOPs** (`sops.html`) — 22 ACOSTA-only `.docx` downloads grouped alphabetically

## Architecture

```
eLSOP/
  build.py             ← Python site generator (reads data/, renders templates/, writes site/)
  data/
    sops.json          ← All SOP records (add new SOPs here)
  templates/
    base.html.j2       ← Shared layout: nav (Home, Library, Dashboard, Customer Matrix, Formal SOPs), dark mode, footer
    index.html.j2      ← Homepage
    library.html.j2    ← SOP library with filters
    reader.html.j2     ← Book reader per SOP
    dashboard.html.j2  ← Dashboard
  static/
    portal.css         ← Full design system (light + dark mode, responsive)
  site/                ← OUTPUT — served by GitHub Pages
    index.html         ← Generated: Knowledge Portal homepage
    library.html       ← Generated: SOP library
    dashboard.html     ← Generated: Dashboard
    customers.html     ← Hand-authored: Customer SOP Matrix page (DO NOT overwrite)
    sops.html          ← Hand-authored: Formal SOPs downloads page (DO NOT overwrite)
    customers.js       ← Generated by rebuild-customers.ps1 from Customer_Matrix.xlsx
    app.js             ← Customer Matrix table logic
    style.css          ← Customer Matrix / standalone pages stylesheet
    portal.css         ← Generated by build.py from static/
    reader/
      SOP-Acosta-001.html
      SOP-Acosta-002.html
      ...
    sops/              ← Formal SOP .docx files (22 customers)
  .github/workflows/
    deploy.yml         ← GitHub Actions: runs build.py then deploys site/
```

> **Important:** `site/customers.html` and `site/sops.html` are hand-authored standalone pages.
> They are **not** generated by `build.py` and must never be overwritten by the build.
> `build.py` will warn if they are missing from `site/`.

## How it works

On every push to `main`, GitHub Actions:
1. Sets up Python 3.12
2. Installs `jinja2`
3. Runs `python build.py` — generates all HTML pages into `site/`
4. Deploys `site/` to GitHub Pages

## GitHub Pages setup

1. Push this repository to GitHub
2. Go to **Settings → Pages**
3. Under **Source**, select **GitHub Actions**
4. Push to `main` — the site deploys automatically

## Adding a new SOP

Edit [`data/sops.json`](data/sops.json) — add a new object with these fields:

```json
{
  "id": "SOP-Acosta-013",
  "title": "Your SOP Title",
  "department": "Claims",
  "category": "Customer SOP",
  "owner": "Acosta Team",
  "version": "1.0",
  "status": "Active",
  "updated": "2025-01-01",
  "created": "2025-01-01",
  "views": 0,
  "description": "Short description shown on library cards.",
  "file": "sops/Your_File.docx",
  "tags": ["tag1", "tag2"],
  "sections": [
    { "id": "purpose",     "title": "Purpose",                 "content": "..." },
    { "id": "scope",       "title": "Scope",                   "content": "..." },
    { "id": "definitions", "title": "Definitions",             "content": "..." },
    { "id": "roles",       "title": "Roles & Responsibilities", "content": "..." },
    { "id": "procedure",   "title": "Procedure Steps",         "content": "..." },
    { "id": "references",  "title": "References",              "content": "..." },
    { "id": "revisions",   "title": "Revision History",        "content": "..." },
    { "id": "related",     "title": "Related Documents",       "content": "..." }
  ]
}
```

Then commit and push — the site rebuilds and deploys automatically.

## Running the build locally

```bash
pip install jinja2
python build.py
```

Then open `site/index.html` in your browser.

## Regenerating customer matrix data

If `Customer_Matrix.xlsx` is updated, regenerate `customers.js`:

```powershell
powershell -ExecutionPolicy Bypass -File "site/rebuild-customers.ps1"
```

Then commit and push the updated `site/customers.js`. The Customer Matrix page (`customers.html`) reads `customers.js` at runtime — no build step needed for data changes.

## Navigation structure

All pages share the same top navigation bar:

| Link | Page | Source |
|------|------|--------|
| Home | `index.html` | Generated by `build.py` |
| SOP Library | `library.html` | Generated by `build.py` |
| Dashboard | `dashboard.html` | Generated by `build.py` |
| Customer Matrix | `customers.html` | Hand-authored standalone |
| Formal SOPs | `sops.html` | Hand-authored standalone |

## Target users

- Customer support agents
- Operations specialists
- Claims and repayment teams
- Team leaders and trainers
